name: PR CI
# 🔴 DISABLED - Use CircleCI for PR validation instead
# This workflow is now disabled; all PR checks run on CircleCI
# See .circleci/config.yml for current PR validation pipeline

on:
  workflow_dispatch: # Only manual trigger if needed

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "❌ PR validation moved to CircleCI - see .circleci/ for config"

