name: Main - Deploy & Release
# 🔴 DISABLED - Use CircleCI for deployment instead
# This workflow is now disabled; all tag-based deployments run on CircleCI
# See .circleci/workflows/main-merge.yml for current deployment pipeline

on:
  workflow_dispatch: # Only manual trigger if needed

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "❌ Deployment moved to CircleCI - see .circleci/workflows/main-merge.yml for config"

