name: PR Validation

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      force_all:
        description: 'Force build all containers'
        required: false
        type: boolean
        default: false

env:
  REGISTRY: ghcr.io
  NODE_VERSION: '20'

permissions:
  contents: read
  packages: write
  pull-requests: write

concurrency:
  group: pr-validation-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  # =============================================================================
  # CORE VALIDATION - Always runs on every PR
  # =============================================================================
  validate:
    name: 🧪 Validate
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build shared package
        run: cd packages/shared && npm run build

      - name: TypeScript check
        run: npm run type-check

      - name: Lint
        run: npm run lint

      - name: Run tests
        run: npm test

  # =============================================================================
  # CHANGE DETECTION - Separate job per container
  # =============================================================================
  detect-changes-bunkbot:
    name: 🔍 Detect Changes - bunkbot
    runs-on: ubuntu-latest
    outputs:
      changed: ${{ steps.changes.outputs.bunkbot }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Detect changed paths
        id: changes
        uses: dorny/paths-filter@v3
        with:
          filters: |
            bunkbot:
              - 'apps/bunkbot/**'
              - 'packages/shared/**'

  detect-changes-djcova:
    name: 🔍 Detect Changes - djcova
    runs-on: ubuntu-latest
    outputs:
      changed: ${{ steps.changes.outputs.djcova }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Detect changed paths
        id: changes
        uses: dorny/paths-filter@v3
        with:
          filters: |
            djcova:
              - 'apps/djcova/**'
              - 'packages/shared/**'

  detect-changes-starbunk-dnd:
    name: 🔍 Detect Changes - starbunk-dnd
    runs-on: ubuntu-latest
    outputs:
      changed: ${{ steps.changes.outputs.starbunk-dnd }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Detect changed paths
        id: changes
        uses: dorny/paths-filter@v3
        with:
          filters: |
            starbunk-dnd:
              - 'apps/starbunk-dnd/**'
              - 'packages/shared/**'

  detect-changes-covabot:
    name: 🔍 Detect Changes - covabot
    runs-on: ubuntu-latest
    outputs:
      changed: ${{ steps.changes.outputs.covabot }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Detect changed paths
        id: changes
        uses: dorny/paths-filter@v3
        with:
          filters: |
            covabot:
              - 'apps/covabot/**'
              - 'packages/shared/**'

  # =============================================================================
  # DOCKER BUILDS - Only for changed containers
  # =============================================================================

  build-bunkbot:
    name: 🐳 Build bunkbot
    runs-on: ubuntu-latest
    needs: [validate, detect-changes-bunkbot]
    if: needs.detect-changes-bunkbot.outputs.changed == 'true' || github.event.inputs.force_all == 'true'
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          file: apps/bunkbot/Dockerfile
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ github.repository_owner }}/bunkbot:pr-${{ github.event.pull_request.number || 'main' }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  build-djcova:
    name: 🐳 Build djcova
    runs-on: ubuntu-latest
    needs: [validate, detect-changes-djcova]
    if: needs.detect-changes-djcova.outputs.changed == 'true' || github.event.inputs.force_all == 'true'
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          file: apps/djcova/Dockerfile
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ github.repository_owner }}/djcova:pr-${{ github.event.pull_request.number || 'main' }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  build-starbunk-dnd:
    name: 🐳 Build starbunk-dnd
    runs-on: ubuntu-latest
    needs: [validate, detect-changes-starbunk-dnd]
    if: needs.detect-changes-starbunk-dnd.outputs.changed == 'true' || github.event.inputs.force_all == 'true'
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          file: apps/starbunk-dnd/Dockerfile
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ github.repository_owner }}/starbunk-dnd:pr-${{ github.event.pull_request.number || 'main' }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  build-covabot:
    name: 🐳 Build covabot
    runs-on: ubuntu-latest
    needs: [validate, detect-changes-covabot]
    if: needs.detect-changes-covabot.outputs.changed == 'true' || github.event.inputs.force_all == 'true'
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          file: apps/covabot/Dockerfile
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ github.repository_owner }}/covabot:pr-${{ github.event.pull_request.number || 'main' }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # =============================================================================
  # SUCCESS GATE - For branch protection
  # =============================================================================
  pr-validation-success:
    name: ✅ PR Validation
    runs-on: ubuntu-latest
    needs: [validate]
    if: always()
    steps:
      - name: Check validation result
        run: |
          if [[ "${{ needs.validate.result }}" == "success" ]]; then
            echo "✅ All validation checks passed"
            exit 0
          else
            echo "❌ Validation failed"
            exit 1
          fi
