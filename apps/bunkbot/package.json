{
	"name": "@starbunk/bunkbot",
	"version": "1.10.1",
	"description": "BunkBot - Next generation reply bot system",
	"main": "dist/index.js",
	"engines": {
		"node": ">=18"
	},
	"scripts": {
		"build": "tsc && tsc-alias",
		"build:docker": "tsc -p tsconfig.docker.json && tsc-alias -p tsconfig.docker.json",
		"start": "node dist/index.js",
		"start:prod": "npm run build && npm run start",
		"dev": "cross-env NODE_ENV=development DEBUG=true ts-node-dev --respawn --transpile-only -r tsconfig-paths/register src/index.ts",
		"dev:watch": "cross-env NODE_ENV=development DEBUG=true ts-node-dev --respawn --transpile-only -r tsconfig-paths/register src/index.ts",
		"type-check": "tsc --noEmit",
		"lint": "eslint \"src/**/*.ts\"",
		"lint:fix": "eslint \"src/**/*.ts\" --fix",
		"clean": "rimraf dist",
		"rebuild": "npm run clean && npm run build",
		"test": "vitest",
		"test:ui": "vitest --ui",
		"test:run": "vitest run",
		"pipeline": "npm run lint && npm run type-check && npm run build && npm run test:run"
	},
	"dependencies": {
		"discord.js": "^14.18.0",
		"dotenv": "^16.5.0",
		"js-yaml": "^4.1.0",
		"prom-client": "^15.1.3",
		"zod": "^3.24.2"
	},
	"devDependencies": {
		"@types/js-yaml": "^4.0.9",
		"@types/node": "^20.17.14",
		"cross-env": "^7.0.3",
		"rimraf": "^5.0.1",
		"ts-node": "^10.9.2",
		"ts-node-dev": "^2.0.0",
		"tsc-alias": "^1.8.11",
		"tsconfig-paths": "^4.2.0",
		"typescript": "~5.3.0"
	}
}
