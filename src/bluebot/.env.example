## Minimal .env example for @starbunk/bluebot
## Copy to .env and fill in only what you actually need.

# -----------------------------------------------------------------------------
# REQUIRED
# -----------------------------------------------------------------------------

# Discord bot token BlueBot will log in with
BLUEBOT_TOKEN=your-bluebot-token-here

# Ollama API endpoint (required if you want AI/LLM behaviour)
OLLAMA_API_URL=http://127.0.0.1:11434

# LLM provider selection for BlueBot (options: "ollama", "gemini")
# If unset or invalid, BlueBot will try Ollama first, then Gemini as a fallback
# LLM_PROVIDER=ollama

# -----------------------------------------------------------------------------
# OPTIONAL OVERRIDES / EXTRAS (UNCOMMENT IF NEEDED)
# -----------------------------------------------------------------------------

# Use these if you prefer to share a common token instead of BLUEBOT_TOKEN
# DISCORD_TOKEN=
# STARBUNK_TOKEN=

# BlueBot Tester token (for testing BlueBot responses without sending to Discord)
# Run with: npm run tester
# BLUEBOT_TESTER_TOKEN=your-tester-bot-token-here

# Interactive CLI Tester identity (for offline testing without Discord)
# Run with: npm run tester:cli
# TEST_USER_ID=123456789012345678
# TEST_USERNAME=TestUser
# TEST_NICKNAME=TestNick

# Explicit Ollama model (defaults to "llama3" if unset)
# OLLAMA_DEFAULT_MODEL=llama3

# Use a single Discord webhook URL instead of per-channel webhooks
# DISCORD_WEBHOOK_URL=

# Enable Gemini provider (not required for basic usage, used when LLM_PROVIDER=gemini)
# GEMINI_API_KEY=
# GEMINI_DEFAULT_MODEL=gemini-2.0-flash-exp

# Health / metrics ports (defaults to 3000 if unset)
# METRICS_PORT=3000

# Observability and debug toggles
# DEBUG_MODE=false
# ENABLE_METRICS=true
# ENABLE_STRUCTURED_LOGGING=true

# CI smoke-test mode (used only in CI pipelines)
# CI_SMOKE_MODE=false

# BlueBot behavior configuration
# Optional: Comma-separated list of Discord user IDs that are on BlueBot's "naughty list"
# BlueBot will be cold, contemptuous, and dismissive toward these users instead of friendly
# Example: BLUEBOT_ENEMY_USER_IDS=123456789012345678,987654321098765432
# BLUEBOT_ENEMY_USER_IDS=

# Optional: Path to a YAML file containing custom response examples for BlueBot
# This allows you to add custom responses without modifying code
# The custom responses are layered on top of the default responses
# Example: BLUEBOT_CUSTOM_RESPONSES_PATH=/path/to/custom-responses.yaml
# BLUEBOT_CUSTOM_RESPONSES_PATH=
