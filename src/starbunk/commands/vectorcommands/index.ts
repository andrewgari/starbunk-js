import addFile from './addFile';
import convertAll from './convertAll';
import search from './search';

// Export all vector commands
const vectorCommands = [
	addFile,
	convertAll,
	search
];

export default vectorCommands;
