# ============================================================================
# MINIMAL ENVIRONMENT CONFIGURATION
# ============================================================================
# This file contains ONLY the absolute minimum required variables to run
# the Starbunk bot stack with docker-compose.
#
# All other variables have sensible defaults in docker-compose.yml.
# For full configuration options, see config/env/.env.example
# ============================================================================

# ----------------------------------------------------------------------------
# REQUIRED: Repository Path
# ----------------------------------------------------------------------------
# Absolute path to the starbunk-js repository root
# Used for volume mounts in docker-compose
HOST_WORKDIR=/path/to/starbunk-js

# ----------------------------------------------------------------------------
# REQUIRED: Discord Bot Tokens
# ----------------------------------------------------------------------------
# Get tokens from: https://discord.com/developers/applications
# You need at least one bot token to run the stack

BUNKBOT_TOKEN=your-bunkbot-token-here
DJCOVA_TOKEN=your-djcova-token-here
COVABOT_TOKEN=your-covabot-token-here
BLUEBOT_TOKEN=your-bluebot-token-here

# DJCova client ID (for slash command registration)
DJCOVA_CLIENT_ID=your-djcova-client-id-here

# ----------------------------------------------------------------------------
# REQUIRED: Remote Observability Stack
# ----------------------------------------------------------------------------
# Point this to your Unraid server (or other remote observability stack)
# Replace 192.168.1.100 with your actual server IP

OBSERVABILITY_HOST=192.168.1.100

# ----------------------------------------------------------------------------
# That's it! Everything else uses defaults.
# ----------------------------------------------------------------------------
# Default services included:
#   - BunkBot, DJCova, CovaBot, BlueBot (bots)
#   - PostgreSQL (starbunk-db)
#   - Redis (starbunk-cache)
#   - Qdrant (starbunk-vectordb)
#   - OTEL Collector (starbunk-exporter)
#
# To deploy:
#   1. Copy this file to .env
#   2. Fill in the values above
#   3. Run: docker-compose up -d
# ============================================================================

