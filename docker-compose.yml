version: '3.7'

services:
  bunkbot-ts:
    image: andrewgari/bunkbot:latest
    build: 
      context: .
      dockerfile: Dockerfile
      target: build
    container_name: bunkbot-js
    env_file: .env
    command: npm run start
