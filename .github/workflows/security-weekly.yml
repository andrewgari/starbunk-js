name: Weekly Security Scan
# 🔒 DISABLED - Security scanning via external services
on:
  workflow_dispatch: # Only run manually if needed

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🔒 Security scanning disabled - use CircleCI/external scanning services"

