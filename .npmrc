# Suppress audit warnings for dev-only vulnerabilities in semantic-release
# All vulnerabilities are in bundled dependencies of @semantic-release/npm
# and only affect the CI/CD release process, not production code.
# Production dependencies have 0 vulnerabilities.
audit=false

