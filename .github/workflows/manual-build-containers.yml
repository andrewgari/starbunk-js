name: Manual - Build Containers

on:
  workflow_dispatch:
    inputs:
      version_override:
        description: 'Override version (leave empty to use VERSION file)'
        type: string
        required: false
        default: ''
      containers:
        description: 'Containers to build (comma-separated: bunkbot,djcova,starbunk-dnd,covabot or "all")'
        type: string
        required: false
        default: 'all'

permissions:
  contents: write
  packages: write

env:
  REGISTRY: ghcr.io
  REGISTRY_PREFIX: ghcr.io/${{ github.repository_owner }}
  NODE_VERSION: '20'

jobs:
  prepare:
    name: 🔍 Prepare Build
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.determine-version.outputs.version }}
      containers: ${{ steps.determine-containers.outputs.containers }}
    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v4
        with:
          ref: main

      - name: 📦 Determine Version
        id: determine-version
        run: |
          if [ -n "${{ github.event.inputs.version_override }}" ]; then
            VERSION="${{ github.event.inputs.version_override }}"
            echo "📦 Using override version: $VERSION"
          else
            if [ ! -f "VERSION" ]; then
              echo "❌ ERROR: VERSION file not found"
              exit 1
            fi
            VERSION=$(tr -d '[:space:]' < VERSION)
            echo "📦 Using VERSION file: $VERSION"
          fi

          # Validate version format (SemVer: X.Y.Z with optional pre-release and build metadata)
          if ! echo "$VERSION" | grep -Eq '^[0-9]+\.[0-9]+\.[0-9]+(-[0-9A-Za-z.-]+)?(\+[0-9A-Za-z.-]+)?$'; then
            echo "❌ ERROR: Invalid version format '$VERSION' (expected X.Y.Z, optionally with pre-release/build metadata)"
            exit 1
          fi

          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: 🎯 Determine Containers
        id: determine-containers
        run: |
          INPUT="${{ github.event.inputs.containers }}"

          if [ "$INPUT" = "all" ] || [ -z "$INPUT" ]; then
            CONTAINERS='["bunkbot", "djcova", "starbunk-dnd", "covabot"]'
            echo "🚀 Building all containers"
          else
            # Convert comma-separated list to JSON array
            IFS=',' read -ra CONTAINER_ARRAY <<< "$INPUT"
            CONTAINERS="["
            for i in "${!CONTAINER_ARRAY[@]}"; do
              container=$(echo "${CONTAINER_ARRAY[$i]}" | xargs)
              if [ $i -gt 0 ]; then
                CONTAINERS+=","
              fi
              CONTAINERS+="\"$container\""
            done
            CONTAINERS+="]"
            echo "🚀 Building selected containers: $CONTAINERS"
          fi

          echo "containers=$CONTAINERS" >> $GITHUB_OUTPUT

  build-containers:
    name: 🏗️ Build ${{ matrix.container }}
    runs-on: ubuntu-latest
    needs: prepare
    strategy:
      fail-fast: false
      matrix:
        container: ${{ fromJSON(needs.prepare.outputs.containers) }}
    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v4
        with:
          ref: main

      - name: 🏗️ Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: 🔐 Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: 📋 Extract Container Metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY_PREFIX }}/${{ matrix.container }}
          tags: |
            type=raw,value=latest
            type=raw,value=v${{ needs.prepare.outputs.version }}
            type=raw,value=${{ needs.prepare.outputs.version }}
          labels: |
            org.opencontainers.image.title=${{ matrix.container }}
            org.opencontainers.image.description=Starbunk Discord Bot - ${{ matrix.container }} container
            org.opencontainers.image.vendor=Starbunk
            org.opencontainers.image.version=${{ needs.prepare.outputs.version }}
            build.manual=true
            build.timestamp=${{ github.run_id }}

      - name: 🏗️ Build and Push Container Image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: apps/${{ matrix.container }}/Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          platforms: linux/amd64
          cache-from: |
            type=gha,scope=${{ matrix.container }}
            type=registry,ref=${{ env.REGISTRY_PREFIX }}/${{ matrix.container }}:latest
          cache-to: |
            type=gha,mode=max,scope=${{ matrix.container }}
          provenance: false
          build-args: |
            BUILDKIT_INLINE_CACHE=1
            BUILD_DATE=${{ github.run_id }}
            VCS_REF=${{ github.sha }}
            VERSION=${{ needs.prepare.outputs.version }}
            NODE_ENV=production
            LOG_LEVEL=info

      - name: ✅ Verify Image
        run: |
          echo "✅ Successfully built and pushed ${{ matrix.container }}"
          echo "📦 Image: ${{ env.REGISTRY_PREFIX }}/${{ matrix.container }}:latest"
          echo "🏷️ Version: ${{ needs.prepare.outputs.version }}"

  tag-commit:
    name: 🏷️ Tag Git Commit
    runs-on: ubuntu-latest
    needs: [prepare, build-containers]
    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
          BUILD_RESULT='${{ needs.build-containers.result }}'

          echo "## 🎉 Manual Container Build Complete" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📦 Version: \`$VERSION\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          if [ "$BUILD_RESULT" = "success" ] && [ -n "$CONTAINERS" ] && echo "$CONTAINERS" | jq empty >/dev/null 2>&1; then
            echo "### 🐳 Built Containers" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "| Container | Tags |" >> $GITHUB_STEP_SUMMARY
            echo "|-----------|------|" >> $GITHUB_STEP_SUMMARY

            # Parse containers JSON array
            echo "$CONTAINERS" | jq -r '.[]' | while read container; do
              echo "| $container | \`latest\`, \`v$VERSION\`, \`$VERSION\` |" >> $GITHUB_STEP_SUMMARY
            done

            echo "" >> $GITHUB_STEP_SUMMARY
            echo "### 🔧 Pull Commands" >> $GITHUB_STEP_SUMMARY
            echo "\`\`\`bash" >> $GITHUB_STEP_SUMMARY
            echo "$CONTAINERS" | jq -r '.[]' | while read container; do
              echo "docker pull ghcr.io/${{ github.repository_owner }}/$container:latest" >> $GITHUB_STEP_SUMMARY
              echo "docker pull ghcr.io/${{ github.repository_owner }}/$container:v$VERSION" >> $GITHUB_STEP_SUMMARY
            done
            echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          else
            echo "### ⚠️ Container build details unavailable" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "- Build result: \`$BUILD_RESULT\`" >> $GITHUB_STEP_SUMMARY
            echo "- Container images and pull commands are not listed because the build did not complete successfully or container metadata is unavailable." >> $GITHUB_STEP_SUMMARY
          fi
  summary:
    name: 📋 Build Summary
    runs-on: ubuntu-latest
    needs: [prepare, build-containers, tag-commit]
    if: always()
    steps:
      - name: 📊 Generate Summary
        run: |
          VERSION="${{ needs.prepare.outputs.version }}"
          CONTAINERS='${{ needs.prepare.outputs.containers }}'
          PREPARE_RESULT="${{ needs.prepare.result }}"
          BUILD_RESULT="${{ needs['build-containers'].result }}"
          TAG_RESULT="${{ needs['tag-commit'].result }}"

          OVERALL_STATUS="success"
          if [ "$PREPARE_RESULT" != "success" ] || [ "$BUILD_RESULT" != "success" ] || [ "$TAG_RESULT" != "success" ]; then
            OVERALL_STATUS="failure"
          fi

          if [ "$OVERALL_STATUS" = "success" ]; then
            echo "## 🎉 Manual Container Build Succeeded" >> "$GITHUB_STEP_SUMMARY"
          else
            echo "## ❌ Manual Container Build Failed" >> "$GITHUB_STEP_SUMMARY"
          fi

          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "### 📦 Version: \`v$VERSION\`" >> "$GITHUB_STEP_SUMMARY"
          echo "### 🏷️ Git Tag: \`v$VERSION\`" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"

          if [ "$OVERALL_STATUS" = "success" ]; then
            echo "### 🐳 Built Containers" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo "| Container | Tags |" >> "$GITHUB_STEP_SUMMARY"
            echo "|-----------|------|" >> "$GITHUB_STEP_SUMMARY"

            # Parse containers JSON array
            echo "$CONTAINERS" | jq -r '.[]' | while read container; do
              echo "| $container | \`latest\`, \`v$VERSION\`, \`$VERSION\` |" >> "$GITHUB_STEP_SUMMARY"
            done

            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo "### 🔧 Pull Commands" >> "$GITHUB_STEP_SUMMARY"
            echo "\`\`\`bash" >> "$GITHUB_STEP_SUMMARY"
            echo "$CONTAINERS" | jq -r '.[]' | while read container; do
              echo "docker pull ghcr.io/${{ github.repository_owner }}/$container:latest" >> "$GITHUB_STEP_SUMMARY"
              echo "docker pull ghcr.io/${{ github.repository_owner }}/$container:v$VERSION" >> "$GITHUB_STEP_SUMMARY"
            done
            echo "\`\`\`" >> "$GITHUB_STEP_SUMMARY"
          else
            echo "One or more jobs failed:" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo "- prepare: \`$PREPARE_RESULT\`" >> "$GITHUB_STEP_SUMMARY"
            echo "- build-containers: \`$BUILD_RESULT\`" >> "$GITHUB_STEP_SUMMARY"
            echo "- tag-commit: \`$TAG_RESULT\`" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo "> Container list and pull commands are omitted because the build/tag process did not fully succeed." >> "$GITHUB_STEP_SUMMARY"
          fi
